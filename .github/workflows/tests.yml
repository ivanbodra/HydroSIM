name: tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pytest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install Qt runtime libraries
        run: sudo apt-get update && sudo apt-get install -y libegl1 libgl1 libxkbcommon-x11-0

      - name: Install package and test dependencies
        run: python -m pip install --upgrade pip && python -m pip install -e '.[dev,visualization]'

      - name: Run Ruff
        run: ruff check .

      - name: Run tests
        env:
          MPLBACKEND: Agg
        run: pytest -q

      - name: Capture real Didactic Explorer windows
        env:
          QT_QPA_PLATFORM: offscreen
          MPLBACKEND: QtAgg
        shell: bash
        run: |
          mkdir -p artifacts
          set +e
          python tools/capture_didactic_explorer.py --output artifacts/didactic_explorer_actual.png > artifacts/capture.log 2>&1
          actual_status=$?
          python tools/capture_didactic_explorer.py --lesson Signal --output artifacts/didactic_explorer_signal.png >> artifacts/capture.log 2>&1
          signal_status=$?
          echo "$actual_status" > artifacts/capture.exit
          echo "$signal_status" > artifacts/capture_signal.exit
          set -e
          test "$actual_status" = "0"
          test "$signal_status" = "0"
          test -s artifacts/didactic_explorer_actual.png
          test -s artifacts/didactic_explorer_signal.png

      - name: Upload Didactic Explorer screenshots and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: didactic-explorer-screenshot
          path: artifacts/

      - name: Publish real screenshots to repository
        if: github.ref == 'refs/heads/ui-shot'
        shell: bash
        run: |
          test "$(cat artifacts/capture.exit)" = "0"
          test "$(cat artifacts/capture_signal.exit)" = "0"
          mkdir -p docs/images
          cp artifacts/didactic_explorer_actual.png docs/images/didactic_explorer_actual.png
          cp artifacts/didactic_explorer_signal.png docs/images/didactic_explorer_signal.png
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/images/didactic_explorer_actual.png docs/images/didactic_explorer_signal.png
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Add real Didactic Explorer screenshots [skip ci]"
          git push origin HEAD:ui-shot
