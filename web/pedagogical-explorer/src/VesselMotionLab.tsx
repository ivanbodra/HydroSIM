import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Languages, Rotate3D, Ship, Waves } from 'lucide-react';
type Lang='en'|'pt';type Harmonic={amplitude_deg:number;period_seconds:number;phase_deg:number};type Sample={time_seconds:number;north_m:number;east_m:number;down_m:number;roll_deg:number;pitch_deg:number;heading_deg:number;yaw_deviation_deg:number;heave_up_m:number};type Response={samples:Sample[];metadata:Record<string,string>};
const copy={en:{title:'Vessel Motion',intro:'Configure deterministic vessel motion and inspect the canonical Truth trajectory and attitude over time.',back:'System Map',lang:'PT-BR',heading:'Heading',speed:'Speed',duration:'Duration',roll:'Roll',pitch:'Pitch',yaw:'Yaw',heave:'Heave',amplitude:'Amplitude',period:'Period',trajectory:'Truth trajectory',attitude:'Attitude through time',north:'North',east:'East',up:'Up',current:'Final sample',unsupported:'Not yet represented by the canonical D12 API',downstream:'Beam displacement and sounding consequences remain intentionally unavailable in this slice.',help:{roll:'Roll is rotation about the vessel longitudinal axis.',pitch:'Pitch is rotation about the vessel transverse axis.',yaw:'Yaw is angular deviation about the vertical axis; Heading is the vessel direction clockwise from North.',heave:'Heave is vertical translation; HydroSIM reports it positive Up.'}},pt:{title:'Movimento da Embarcação',intro:'Configure o movimento determinístico da embarcação e inspecione a trajetória Truth e a atitude canônicas ao longo do tempo.',back:'Mapa do Sistema',lang:'EN',heading:'Heading',speed:'Velocidade',duration:'Duração',roll:'Roll',pitch:'Pitch',yaw:'Yaw',heave:'Heave',amplitude:'Amplitude',period:'Período',trajectory:'Trajetória Truth',attitude:'Atitude ao longo do tempo',north:'Norte',east:'Leste',up:'Cima',current:'Amostra final',unsupported:'Ainda não representado pela API canônica D12',downstream:'Deslocamento do feixe e consequências nas sondagens permanecem intencionalmente indisponíveis neste slice.',help:{roll:'Roll é a rotação em torno do eixo longitudinal da embarcação.',pitch:'Pitch é a rotação em torno do eixo transversal da embarcação.',yaw:'Yaw é o desvio angular em torno do eixo vertical; Heading é a direção da embarcação, medida no sentido horário a partir do Norte.',heave:'Heave é a translação vertical; no HydroSIM é apresentada positiva para cima.'}}};
function Term({label,text}:{label:string;text:string}){const[open,setOpen]=useState(false);return <span className="d12-term"><button type="button" onClick={()=>setOpen(v=>!v)} title={text}>{label} ⓘ</button>{open&&<span>{text}</span>}</span>}function seriesPath(samples:Sample[],key:keyof Sample,w=660,h=170){if(!samples.length)return'';const vals=samples.map(s=>Number(s[key]));const min=Math.min(...vals),max=Math.max(...vals),span=Math.max(1e-9,max-min);return vals.map((v,i)=>`${i?'L':'M'}${(i/(vals.length-1))*w},${h-((v-min)/span)*h}`).join(' ')}
export default function VesselMotionLab({onBack}:{onBack:()=>void}){const[lang,setLang]=useState<Lang>('en');const[heading,setHeading]=useState(20);const[speed,setSpeed]=useState(3);const[duration,setDuration]=useState(12);const[roll,setRoll]=useState<Harmonic>({amplitude_deg:8,period_seconds:6,phase_deg:0});const[pitch,setPitch]=useState<Harmonic>({amplitude_deg:4,period_seconds:8,phase_deg:30});const[yaw,setYaw]=useState<Harmonic>({amplitude_deg:6,period_seconds:10,phase_deg:0});const[heaveAmp,setHeaveAmp]=useState(1.2);const[heavePeriod,setHeavePeriod]=useState(7);const[data,setData]=useState<Response|null>(null);const[error,setError]=useState('');const t=copy[lang];useEffect(()=>{const ac=new AbortController();setError('');fetch('/api/v1/pedagogical/vessel-motion',{method:'POST',headers:{'Content-Type':'application/json'},signal:ac.signal,body:JSON.stringify({heading_deg:heading,speed_mps:speed,start_north_m:0,start_east_m:0,start_down_m:0,duration_seconds:duration,sample_count:121,roll,pitch,yaw_deviation:yaw,heave:{amplitude_m:heaveAmp,period_seconds:heavePeriod,phase_deg:0}})}).then(async r=>{if(!r.ok)throw new Error(await r.text());return r.json() as Promise<Response>}).then(setData).catch(e=>{if(e.name!=='AbortError')setError(String(e))});return()=>ac.abort()},[heading,speed,duration,roll,pitch,yaw,heaveAmp,heavePeriod]);const path=useMemo(()=>{const s=data?.samples??[];if(!s.length)return[];const ns=s.map(x=>x.north_m),es=s.map(x=>x.east_m);const n0=Math.min(...ns),n1=Math.max(...ns),e0=Math.min(...es),e1=Math.max(...es),dn=Math.max(1e-9,n1-n0),de=Math.max(1e-9,e1-e0);return s.map(x=>({x:8+84*(x.east_m-e0)/de,y:92-84*(x.north_m-n0)/dn}))},[data]);const last=data?.samples.at(-1);const harmonic=(label:string,value:Harmonic,setter:(v:Harmonic)=>void,help:string)=><section><h3><Term label={label} text={help}/></h3><label>{t.amplitude}<strong>{value.amplitude_deg.toFixed(0)}°</strong><input type="range" min="0" max="20" step="1" value={value.amplitude_deg} onChange={e=>setter({...value,amplitude_deg:+e.target.value})}/></label><label>{t.period}<strong>{value.period_seconds.toFixed(1)} s</strong><input type="range" min="2" max="16" step="0.5" value={value.period_seconds} onChange={e=>setter({...value,period_seconds:+e.target.value})}/></label></section>;return <div className="d12-lab"><header><button onClick={onBack}><ArrowLeft size={16}/>{t.back}</button><div><span>PED-D12 · VESSEL MOTION</span><h1>{t.title}</h1><p>{t.intro}</p></div><button onClick={()=>setLang(lang==='en'?'pt':'en')}><Languages size={16}/>{t.lang}</button></header><main><aside><label>{t.heading}<strong>{heading.toFixed(0)}°</strong><input type="range" min="0" max="359" value={heading} onChange={e=>setHeading(+e.target.value)}/></label><label>{t.speed}<strong>{speed.toFixed(1)} m/s</strong><input type="range" min="0" max="8" step="0.25" value={speed} onChange={e=>setSpeed(+e.target.value)}/></label><label>{t.duration}<strong>{duration.toFixed(0)} s</strong><input type="range" min="4" max="30" step="1" value={duration} onChange={e=>setDuration(+e.target.value)}/></label>{harmonic(t.roll,roll,setRoll,t.help.roll)}{harmonic(t.pitch,pitch,setPitch,t.help.pitch)}{harmonic(t.yaw,yaw,setYaw,t.help.yaw)}<section><h3><Term label={t.heave} text={t.help.heave}/></h3><label>{t.amplitude}<strong>{heaveAmp.toFixed(1)} m</strong><input type="range" min="0" max="3" step="0.1" value={heaveAmp} onChange={e=>setHeaveAmp(+e.target.value)}/></label><label>{t.period}<strong>{heavePeriod.toFixed(1)} s</strong><input type="range" min="2" max="16" step="0.5" value={heavePeriod} onChange={e=>setHeavePeriod(+e.target.value)}/></label></section></aside><section className="d12-stage">{error?<div className="d12-error">{error}</div>:<><div className="d12-panels"><article><div className="d12-title"><Ship size={17}/><span>{t.trajectory}</span></div><div className="d12-map"><i className="north">N</i><svg viewBox="0 0 100 100">{path.length>1&&<polyline points={path.map(p=>`${p.x},${p.y}`).join(' ')} />}{path.length>0&&<circle cx={path.at(-1)!.x} cy={path.at(-1)!.y} r="2.2"/>}</svg></div><div className="d12-read"><span>{t.north}<strong>{last?.north_m.toFixed(1)??'—'} m</strong></span><span>{t.east}<strong>{last?.east_m.toFixed(1)??'—'} m</strong></span></div></article><article><div className="d12-title"><Rotate3D size={17}/><span>{t.attitude}</span></div><svg className="d12-series" viewBox="0 0 660 170"><path className="roll" d={seriesPath(data?.samples??[],'roll_deg')}/><path className="pitch" d={seriesPath(data?.samples??[],'pitch_deg')}/><path className="yaw" d={seriesPath(data?.samples??[],'yaw_deviation_deg')}/></svg><div className="d12-legend"><span className="roll">Roll</span><span className="pitch">Pitch</span><span className="yaw">Yaw</span></div></article></div><article className="d12-heave"><div className="d12-title"><Waves size={17}/><span><Term label={t.heave} text={t.help.heave}/></span></div><svg className="d12-series" viewBox="0 0 660 120"><path className="heave" d={seriesPath(data?.samples??[],'heave_up_m',660,120)}/></svg><div className="d12-read"><span>{t.current}<strong>{last?.heading_deg.toFixed(1)??'—'}° Heading</strong></span><span>{t.up}<strong>{last?.heave_up_m.toFixed(2)??'—'} m</strong></span></div></article><div className="d12-limit"><strong>{t.unsupported}</strong><span>{t.downstream}</span></div></>}</section></main></div>}
