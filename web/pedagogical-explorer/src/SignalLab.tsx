import { RotateCcw, SlidersHorizontal } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';

type PulseType='cw'|'lfm';
type ChirpDirection='up'|'down';
type EnvelopeModel='rectangular'|'tukey';
type Trace={x:number[];y:number[];x_unit:string;y_unit:string};
type SignalResponse={pulse_type:PulseType;waveform:Trace;instantaneous_frequency:Trace;matched_filter:Trace;metadata:Record<string,number|string>};
type Locale='en'|'pt-BR';
type Domain={min:number;max:number};
type SignalConfig={pulse_type:PulseType;center_frequency_khz:number;duration_ms:number;bandwidth_khz:number;chirp_direction:ChirpDirection;envelope_model:EnvelopeModel};

const API_BASE=(import.meta.env.VITE_HYDROSIM_API_BASE as string|undefined)??'http://127.0.0.1:8000';
const initial:SignalConfig={pulse_type:'lfm',center_frequency_khz:200,duration_ms:3,bandwidth_khz:20,chirp_direction:'up',envelope_model:'rectangular'};
const WAVE_DOMAIN:Domain={min:-1.05,max:1.05};
const MATCHED_DOMAIN:Domain={min:-1.05,max:1.05};

const text={
 en:{controls:'Signal controls',waveform:'Waveform',frequency:'Centre frequency',duration:'Pulse duration',bandwidth:'LFM bandwidth',direction:'Sweep direction',envelope:'Envelope',up:'Up',down:'Down',rectangular:'Rectangular',tukey:'Tukey',reset:'Reset',passband:'Acoustic waveform',instant:'Instantaneous frequency',matched:'Matched-filter response',loading:'Updating…',offline:'Signal response unavailable',retry:'Retry',timeAxis:'Time (ms)',amplitudeAxis:'Normalized amplitude',frequencyAxis:'Frequency (kHz)',lagAxis:'Lag (µs)'},
 'pt-BR':{controls:'Controles do sinal',waveform:'Forma de onda',frequency:'Frequência central',duration:'Duração do pulso',bandwidth:'Largura de banda LFM',direction:'Direção da varredura',envelope:'Envoltória',up:'Ascendente',down:'Descendente',rectangular:'Retangular',tukey:'Tukey',reset:'Restaurar',passband:'Forma de onda acústica',instant:'Frequência instantânea',matched:'Resposta do filtro casado',loading:'Atualizando…',offline:'Resposta do sinal indisponível',retry:'Tentar novamente',timeAxis:'Tempo (ms)',amplitudeAxis:'Amplitude normalizada',frequencyAxis:'Frequência (kHz)',lagAxis:'Atraso (µs)'}
} as const;
const initialLanguage=():Locale=>sessionStorage.getItem('hydrosim-language')==='pt'?'pt-BR':'en';

function tracePath(trace:Trace|undefined,xDomain:Domain,yDomain:Domain,width=760,height=180){if(!trace||trace.x.length<2||trace.x.length!==trace.y.length)return'';const dx=xDomain.max-xDomain.min||1,dy=yDomain.max-yDomain.min||1;return trace.x.map((x,i)=>{const px=((x-xDomain.min)/dx)*width;const py=height-((trace.y[i]-yDomain.min)/dy)*height;return `${i?'L':'M'} ${px.toFixed(2)} ${py.toFixed(2)}`}).join(' ')}
function Plot({trace,xDomain,yDomain,xTicks,yTicks,xLabel,yLabel,className='wave-path'}:{trace?:Trace;xDomain:Domain;yDomain:Domain;xTicks:string[];yTicks:string[];xLabel:string;yLabel:string;className?:string}){const d=useMemo(()=>tracePath(trace,xDomain,yDomain),[trace,xDomain.min,xDomain.max,yDomain.min,yDomain.max]);return <div className="scientific-plot clean-plot"><div className="plot-y-label">{yLabel}</div><div className="plot-y-ticks"><span>{yTicks[0]}</span><span>{yTicks[1]}</span><span>{yTicks[2]}</span></div><svg viewBox="0 0 760 180" preserveAspectRatio="none" aria-hidden="true"><path className="plot-grid" d="M0 45H760 M0 90H760 M0 135H760 M190 0V180 M380 0V180 M570 0V180"/><path className="gridline" d="M0 90H760"/><path className={className} d={d}/></svg><div className="fixed-axis">{xTicks.map(v=><span key={v}>{v}</span>)}</div><div className="plot-x-label">{xLabel}</div></div>}

export default function SignalLab({onBack:_onBack,focus}:{onBack:()=>void;focus?:string}){
 const[locale,setLocale]=useState<Locale>(initialLanguage);const[config,setConfig]=useState<SignalConfig>(initial);const[data,setData]=useState<SignalResponse|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[requestVersion,setRequestVersion]=useState(0);const t=text[locale];
 useEffect(()=>{const onLanguage=(event:Event)=>{const next=(event as CustomEvent<'en'|'pt'>).detail;if(next==='en'||next==='pt')setLocale(next==='pt'?'pt-BR':'en')};window.addEventListener('hydrosim-language-change',onLanguage);return()=>window.removeEventListener('hydrosim-language-change',onLanguage)},[]);
 useEffect(()=>{const controller=new AbortController();const timer=window.setTimeout(async()=>{setLoading(true);setError(null);try{const response=await fetch(`${API_BASE}/api/v1/pedagogical/signal`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(config),signal:controller.signal});if(!response.ok)throw new Error(`HTTP ${response.status}`);setData(await response.json() as SignalResponse)}catch(err){if(!controller.signal.aborted)setError(err instanceof Error?err.message:String(err))}finally{if(!controller.signal.aborted)setLoading(false)}},70);return()=>{window.clearTimeout(timer);controller.abort()}},[config,requestVersion]);
 const patch=(next:Partial<SignalConfig>)=>setConfig(current=>({...current,...next}));
 const setPulseType=(pulse_type:PulseType)=>setConfig(current=>{const duration_ms=pulse_type==='cw'?Math.min(.5,Math.max(.02,current.duration_ms)):Math.min(12,Math.max(.3,current.duration_ms));return{...current,pulse_type,duration_ms}});
 const active=focus==='spectrum'?'frequency':focus==='pulse'?'overview':focus??'overview';
 const showWave=active==='overview'||active==='waveform';const showFrequency=active==='overview'||active==='frequency';const showCompression=active==='overview'||active==='compression';
 const waveWindow=Math.min(config.duration_ms,Math.max(.02,8/config.center_frequency_khz));
 const waveDomain:Domain={min:0,max:waveWindow};
 const waveTicks=['0',(waveWindow/2).toFixed(3),waveWindow.toFixed(3)];
 const pulseDomain:Domain={min:0,max:config.duration_ms};
 const pulseTicks=['0',(config.duration_ms/2).toFixed(config.duration_ms<1?2:1),config.duration_ms.toFixed(config.duration_ms<1?2:1)];
 const sweepHalf=config.pulse_type==='lfm'?config.bandwidth_khz/2:0;
 const frequencyPad=Math.max(5,config.pulse_type==='lfm'?config.bandwidth_khz*.15:config.center_frequency_khz*.025);
 const frequencyDomain:Domain={min:Math.max(0,config.center_frequency_khz-sweepHalf-frequencyPad),max:config.center_frequency_khz+sweepHalf+frequencyPad};
 const frequencyTicks=[frequencyDomain.max,config.center_frequency_khz,frequencyDomain.min].map(v=>v.toFixed(v<100?1:0));
 const matchedSpanUs=Math.max(200,config.duration_ms*1000);
 const matchedLagDomain:Domain={min:-matchedSpanUs,max:matchedSpanUs};
 const matchedTicks=[`−${matchedSpanUs.toFixed(0)}`,'0',`+${matchedSpanUs.toFixed(0)}`];
 const durationMin=config.pulse_type==='cw'?.02:.3;
 const durationMax=config.pulse_type==='cw'?.5:12;
 const durationStep=config.pulse_type==='cw'?.01:.1;
 return <div className={`signal-lab signal-focus-${active}`}><div className="lab-layout"><aside className="control-surface"><div className="control-title"><SlidersHorizontal size={17}/><strong>{t.controls}</strong></div><label>{t.waveform}<div className="segmented"><button className={config.pulse_type==='cw'?'on':''} onClick={()=>setPulseType('cw')}>CW</button><button className={config.pulse_type==='lfm'?'on':''} onClick={()=>setPulseType('lfm')}>LFM / Chirp</button></div></label><label>{t.frequency} <output>{config.center_frequency_khz.toFixed(0)} kHz</output><input type="range" min="200" max="700" step="10" value={config.center_frequency_khz} onChange={e=>patch({center_frequency_khz:+e.target.value})}/></label><label>{t.duration} <output>{config.pulse_type==='cw'?(config.duration_ms*1000).toFixed(0)+' µs':config.duration_ms.toFixed(1)+' ms'}</output><input type="range" min={durationMin} max={durationMax} step={durationStep} value={config.duration_ms} onChange={e=>patch({duration_ms:+e.target.value})}/></label><label className={config.pulse_type==='cw'?'disabled-control':''}>{t.bandwidth} <output>{config.bandwidth_khz.toFixed(0)} kHz</output><input disabled={config.pulse_type==='cw'} type="range" min="1" max="100" step="1" value={config.bandwidth_khz} onChange={e=>patch({bandwidth_khz:+e.target.value})}/></label><label className={config.pulse_type==='cw'?'disabled-control':''}>{t.direction}<div className="segmented"><button disabled={config.pulse_type==='cw'} className={config.chirp_direction==='up'?'on':''} onClick={()=>patch({chirp_direction:'up'})}>{t.up}</button><button disabled={config.pulse_type==='cw'} className={config.chirp_direction==='down'?'on':''} onClick={()=>patch({chirp_direction:'down'})}>{t.down}</button></div></label><label>{t.envelope}<div className="segmented"><button className={config.envelope_model==='rectangular'?'on':''} onClick={()=>patch({envelope_model:'rectangular'})}>{t.rectangular}</button><button className={config.envelope_model==='tukey'?'on':''} onClick={()=>patch({envelope_model:'tukey'})}>{t.tukey}</button></div></label><button className="reset" onClick={()=>setConfig(initial)}><RotateCcw size={15}/> {t.reset}</button>{loading&&<div className="scientific-status">{t.loading}</div>}{error&&<div className="scientific-error"><strong>{t.offline}</strong><button onClick={()=>setRequestVersion(v=>v+1)}>{t.retry}</button></div>}</aside><section className="visual-chain signal-continuous clean-signal-plots">{showWave&&<div className="chain-card transmit"><header><div><strong>{t.passband}</strong></div><span>{config.pulse_type==='lfm'?'LFM':'CW'}</span></header><Plot trace={data?.waveform} xDomain={waveDomain} yDomain={WAVE_DOMAIN} xTicks={waveTicks} yTicks={['+1','0','−1']} xLabel={t.timeAxis} yLabel={t.amplitudeAxis}/></div>}{showFrequency&&<div className="chain-card receive"><header><div><strong>{t.instant}</strong></div><span>{config.pulse_type==='lfm'?config.chirp_direction.toUpperCase():'CW'}</span></header><Plot trace={data?.instantaneous_frequency} xDomain={pulseDomain} yDomain={frequencyDomain} className="echo-path" xTicks={pulseTicks} yTicks={frequencyTicks} xLabel={t.timeAxis} yLabel={t.frequencyAxis}/></div>}{showCompression&&<div className="chain-card compression"><header><div><strong>{t.matched}</strong></div></header><Plot trace={data?.matched_filter} xDomain={matchedLagDomain} yDomain={MATCHED_DOMAIN} xTicks={matchedTicks} yTicks={['+1','0','−1']} xLabel={t.lagAxis} yLabel={t.amplitudeAxis}/></div>}</section></div></div>
}
