import { RotateCcw, SlidersHorizontal } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';

type PulseType='cw'|'lfm';
type ChirpDirection='up'|'down';
type EnvelopeModel='rectangular'|'tukey';
type Trace={x:number[];y:number[];x_unit:string;y_unit:string};
type SignalResponse={pulse_type:PulseType;waveform:Trace;instantaneous_frequency:Trace;matched_filter:Trace;ideal_range_resolution_m:number;range_resolution_basis:'cw_pulse_duration'|'lfm_swept_bandwidth';relative_energy:number;relative_energy_reference_duration_ms:number;sound_speed_mps:number;metadata:Record<string,number|string>};
type Locale='en'|'pt-BR';
type Domain={min:number;max:number};
type SignalConfig={pulse_type:PulseType;center_frequency_khz:number;duration_ms:number;bandwidth_khz:number;chirp_direction:ChirpDirection;envelope_model:EnvelopeModel};
const API_BASE=(import.meta.env.VITE_HYDROSIM_API_BASE as string|undefined)??'http://127.0.0.1:8000';
const initial:SignalConfig={pulse_type:'cw',center_frequency_khz:200,duration_ms:.1,bandwidth_khz:20,chirp_direction:'up',envelope_model:'rectangular'};
const WAVE_DOMAIN={min:-1.05,max:1.05},MATCHED_DOMAIN={min:-1.05,max:1.05},CW_TIME_DOMAIN={min:0,max:.5},LFM_TIME_DOMAIN={min:0,max:12},FREQUENCY_DOMAIN={min:140,max:760},MATCHED_LAG_DOMAIN={min:-1500,max:1500};
const text={
 en:{controls:'Signal controls',waveform:'Waveform',frequency:'Centre frequency',duration:'Pulse duration',bandwidth:'LFM bandwidth',direction:'Sweep direction',envelope:'Envelope',up:'Up',down:'Down',rectangular:'Rectangular',tukey:'Tukey',more:'More signal controls',reset:'Reset',passband:'Transmitted pulse',instant:'Frequency law',matched:'Matched-filter output',resolution:'Ideal range resolution',energy:'Relative pulse energy',energyRef:'vs 1 ms rectangular pulse',cwBasis:'set by pulse duration',lfmBasis:'set by swept bandwidth',loading:'Updating…',offline:'Signal response unavailable',retry:'Retry',timeAxis:'Time (ms)',amplitudeAxis:'Normalized amplitude',frequencyAxis:'Frequency (kHz)',lagAxis:'Lag (µs)',story:'ONE PULSE · THREE LINKED VIEWS',step1:'1 · Emit',step2:'2 · Encode',step3:'3 · Compress',consequence:'Acquisition consequence'},
 'pt-BR':{controls:'Controles do sinal',waveform:'Forma de onda',frequency:'Frequência central',duration:'Duração do pulso',bandwidth:'Largura de banda LFM',direction:'Direção da varredura',envelope:'Envoltória',up:'Ascendente',down:'Descendente',rectangular:'Retangular',tukey:'Tukey',more:'Mais controles de sinal',reset:'Restaurar',passband:'Pulso transmitido',instant:'Lei de frequência',matched:'Saída do filtro casado',resolution:'Resolução ideal em distância',energy:'Energia relativa do pulso',energyRef:'vs pulso retangular de 1 ms',cwBasis:'definida pela duração do pulso',lfmBasis:'definida pela largura de banda varrida',loading:'Atualizando…',offline:'Resposta do sinal indisponível',retry:'Tentar novamente',timeAxis:'Tempo (ms)',amplitudeAxis:'Amplitude normalizada',frequencyAxis:'Frequência (kHz)',lagAxis:'Atraso (µs)',story:'UM PULSO · TRÊS VISTAS LIGADAS',step1:'1 · Emitir',step2:'2 · Codificar',step3:'3 · Comprimir',consequence:'Consequência na aquisição'}
} as const;
const initialLanguage=():Locale=>sessionStorage.getItem('hydrosim-language')==='pt'?'pt-BR':'en';
function tracePath(trace:Trace|undefined,xDomain:Domain,yDomain:Domain,width=760,height=180){if(!trace||trace.x.length<2||trace.x.length!==trace.y.length)return'';const dx=xDomain.max-xDomain.min||1,dy=yDomain.max-yDomain.min||1;return trace.x.map((x,i)=>`${i?'L':'M'} ${(((x-xDomain.min)/dx)*width).toFixed(2)} ${(height-((trace.y[i]-yDomain.min)/dy)*height).toFixed(2)}`).join(' ')}
function Plot({trace,xDomain,yDomain,xTicks,yTicks,xLabel,yLabel,className='wave-path'}:{trace?:Trace;xDomain:Domain;yDomain:Domain;xTicks:string[];yTicks:string[];xLabel:string;yLabel:string;className?:string}){const d=useMemo(()=>tracePath(trace,xDomain,yDomain),[trace,xDomain.min,xDomain.max,yDomain.min,yDomain.max]);return <div className="scientific-plot clean-plot"><div className="plot-y-label">{yLabel}</div><div className="plot-y-ticks"><span>{yTicks[0]}</span><span>{yTicks[1]}</span><span>{yTicks[2]}</span></div><svg viewBox="0 0 760 180" preserveAspectRatio="none" aria-hidden="true"><path className="plot-grid" d="M0 45H760 M0 90H760 M0 135H760 M190 0V180 M380 0V180 M570 0V180"/><path className="gridline" d="M0 90H760"/><path className={className} d={d}/></svg><div className="fixed-axis">{xTicks.map(v=><span key={v}>{v}</span>)}</div><div className="plot-x-label">{xLabel}</div></div>}
function Stage({step,title,badge,children}:{step:string;title:string;badge?:string;children:React.ReactNode}){return <section className="signal-story-stage"><header><div><small>{step}</small><strong>{title}</strong></div>{badge&&<span>{badge}</span>}</header>{children}</section>}
export default function SignalLab({onBack:_onBack,focus}:{onBack:()=>void;focus?:string}){
 const[locale,setLocale]=useState<Locale>(initialLanguage);const[config,setConfig]=useState<SignalConfig>(initial);const[data,setData]=useState<SignalResponse|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[requestVersion,setRequestVersion]=useState(0);const t=text[locale];
 useEffect(()=>{const f=(e:Event)=>{const n=(e as CustomEvent<'en'|'pt'>).detail;if(n==='en'||n==='pt')setLocale(n==='pt'?'pt-BR':'en')};window.addEventListener('hydrosim-language-change',f);return()=>window.removeEventListener('hydrosim-language-change',f)},[]);
 useEffect(()=>{const c=new AbortController();const timer=window.setTimeout(async()=>{setLoading(true);setError(null);try{const r=await fetch(`${API_BASE}/api/v1/pedagogical/signal`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(config),signal:c.signal});if(!r.ok)throw new Error(`HTTP ${r.status}`);setData(await r.json() as SignalResponse)}catch(e){if(!c.signal.aborted)setError(e instanceof Error?e.message:String(e))}finally{if(!c.signal.aborted)setLoading(false)}},70);return()=>{clearTimeout(timer);c.abort()}},[config,requestVersion]);
 const patch=(n:Partial<SignalConfig>)=>setConfig(v=>({...v,...n}));const setPulseType=(pulse_type:PulseType)=>setConfig(v=>({...v,pulse_type,duration_ms:pulse_type==='cw'?Math.min(.5,Math.max(.02,v.duration_ms)):Math.min(12,Math.max(.3,v.duration_ms))}));const active=focus==='spectrum'?'frequency':focus==='pulse'?'overview':focus??'overview';const showWave=active==='overview'||active==='waveform',showFrequency=active==='overview'||active==='frequency',showCompression=active==='overview'||active==='compression';const timeDomain=config.pulse_type==='cw'?CW_TIME_DOMAIN:LFM_TIME_DOMAIN;const timeTicks=[timeDomain.min,(timeDomain.max-timeDomain.min)/2,timeDomain.max].map(v=>v.toFixed(config.pulse_type==='cw'?2:0));const frequencyTicks=[FREQUENCY_DOMAIN.max,(FREQUENCY_DOMAIN.max+FREQUENCY_DOMAIN.min)/2,FREQUENCY_DOMAIN.min].map(v=>v.toFixed(0));const durationMin=config.pulse_type==='cw'?.02:.3,durationMax=config.pulse_type==='cw'?.5:12,durationStep=config.pulse_type==='cw'?.01:.1;
 return <div className={`signal-lab signal-focus-${active}`}><div className="lab-layout"><aside className="control-surface"><div className="control-title"><SlidersHorizontal size={17}/><strong>{t.controls}</strong></div><label>{t.waveform}<div className="segmented"><button className={config.pulse_type==='cw'?'on':''} onClick={()=>setPulseType('cw')}>CW</button><button className={config.pulse_type==='lfm'?'on':''} onClick={()=>setPulseType('lfm')}>LFM / Chirp</button></div></label><label>{t.duration} <output>{config.pulse_type==='cw'?(config.duration_ms*1000).toFixed(0)+' µs':config.duration_ms.toFixed(1)+' ms'}</output><input aria-label={t.duration} type="range" min={durationMin} max={durationMax} step={durationStep} value={config.duration_ms} onChange={e=>patch({duration_ms:+e.target.value})}/></label><label className={config.pulse_type==='cw'?'disabled-control':''}>{t.bandwidth} <output>{config.bandwidth_khz.toFixed(0)} kHz</output><input aria-label={t.bandwidth} disabled={config.pulse_type==='cw'} type="range" min="1" max="100" step="1" value={config.bandwidth_khz} onChange={e=>patch({bandwidth_khz:+e.target.value})}/></label><label>{t.frequency} <output>{config.center_frequency_khz.toFixed(0)} kHz</output><input aria-label={t.frequency} type="range" min="200" max="700" step="10" value={config.center_frequency_khz} onChange={e=>patch({center_frequency_khz:+e.target.value})}/></label><details><summary>{t.more}</summary><label className={config.pulse_type==='cw'?'disabled-control':''}>{t.direction}<div className="segmented"><button disabled={config.pulse_type==='cw'} className={config.chirp_direction==='up'?'on':''} onClick={()=>patch({chirp_direction:'up'})}>{t.up}</button><button disabled={config.pulse_type==='cw'} className={config.chirp_direction==='down'?'on':''} onClick={()=>patch({chirp_direction:'down'})}>{t.down}</button></div></label><label>{t.envelope}<div className="segmented"><button className={config.envelope_model==='rectangular'?'on':''} onClick={()=>patch({envelope_model:'rectangular'})}>{t.rectangular}</button><button className={config.envelope_model==='tukey'?'on':''} onClick={()=>patch({envelope_model:'tukey'})}>{t.tukey}</button></div></label></details><button className="reset" onClick={()=>setConfig(initial)}><RotateCcw size={15}/> {t.reset}</button>{loading&&<div className="scientific-status">{t.loading}</div>}{error&&<div className="scientific-error"><strong>{t.offline}</strong><button onClick={()=>setRequestVersion(v=>v+1)}>{t.retry}</button></div>}</aside><main className="signal-story"><div className="signal-story-kicker">{t.story}</div>{showWave&&<Stage step={t.step1} title={t.passband} badge={config.pulse_type==='lfm'?'LFM':'CW'}><Plot trace={data?.waveform} xDomain={timeDomain} yDomain={WAVE_DOMAIN} xTicks={timeTicks} yTicks={['+1','0','−1']} xLabel={t.timeAxis} yLabel={t.amplitudeAxis}/></Stage>}{showWave&&showFrequency&&<div className="signal-story-connector" aria-hidden="true">↓</div>}{showFrequency&&<Stage step={t.step2} title={t.instant} badge={config.pulse_type==='lfm'?config.chirp_direction.toUpperCase():'CW'}><Plot trace={data?.instantaneous_frequency} xDomain={timeDomain} yDomain={FREQUENCY_DOMAIN} className="echo-path" xTicks={timeTicks} yTicks={frequencyTicks} xLabel={t.timeAxis} yLabel={t.frequencyAxis}/></Stage>}{showFrequency&&showCompression&&<div className="signal-story-connector" aria-hidden="true">↓</div>}{showCompression&&<Stage step={t.step3} title={t.matched}><Plot trace={data?.matched_filter} xDomain={MATCHED_LAG_DOMAIN} yDomain={MATCHED_DOMAIN} xTicks={['−1500','0','+1500']} yTicks={['+1','0','−1']} xLabel={t.lagAxis} yLabel={t.amplitudeAxis}/></Stage>}<section className="signal-consequence"><small>{t.consequence}</small><div className="signal-metrics"><article><span>{t.resolution}</span><strong>{data?`${data.ideal_range_resolution_m.toFixed(data.ideal_range_resolution_m<.1?3:2)} m`:'—'}</strong><em>{data?.range_resolution_basis==='lfm_swept_bandwidth'?t.lfmBasis:t.cwBasis}</em></article><article><span>{t.energy}</span><strong>{data?`${data.relative_energy.toFixed(2)}×`:'—'}</strong><em>{t.energyRef}</em></article></div></section></main></div></div>}
