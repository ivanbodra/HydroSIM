import { expect, test } from '@playwright/test';

const signalResponse={pulse_type:'lfm',waveform:{x:[0,.01,.02,.03,.04],y:[0,1,0,-1,0],x_unit:'ms',y_unit:'relative amplitude'},instantaneous_frequency:{x:[0,.5,1],y:[150,200,250],x_unit:'ms',y_unit:'kHz'},matched_filter:{x:[-1000,0,1000],y:[.1,1,.1],x_unit:'us',y_unit:'normalized amplitude'},ideal_range_resolution_m:1.5,range_resolution_basis:'lfm_swept_bandwidth',relative_energy:.5,relative_energy_reference_duration_ms:1,sound_speed_mps:1500,metadata:{chirp_direction:'up'}};

async function setRangeValue(locator:import('@playwright/test').Locator,value:string){await locator.evaluate((element,next)=>{const input=element as HTMLInputElement;const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set;if(!setter)throw new Error('HTMLInputElement value setter unavailable');setter.call(input,next);input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}))},value)}

test('PED-D2 pulse duration updates the API while waveform remains legible',async({page})=>{const requests:Array<Record<string,unknown>>=[];await page.route('**/api/v1/pedagogical/signal',async route=>{requests.push(route.request().postDataJSON() as Record<string,unknown>);await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify(signalResponse)})});await page.goto('/#signal-lab/waveform');await expect(page.getByText('Acoustic waveform',{exact:true})).toBeVisible();await expect(page.getByText('Time (ms)',{exact:true})).toBeVisible();await page.getByRole('button',{name:'LFM / Chirp',exact:true}).click();const duration=page.locator('label').filter({hasText:'Pulse duration'}).locator('input[type="range"]');await setRangeValue(duration,'12');await expect.poll(()=>requests.at(-1)?.duration_ms).toBe(12);await setRangeValue(duration,'.3');await expect.poll(()=>requests.at(-1)?.duration_ms).toBe(.3);});
